# Changelog

## Unreleased

- 手动调节指针/滚轮速度（菜单滑块、恢复默认、键盘 ←/→）或在外部（如 Windows 设置）修改速度后，当前生效规则失效：生效规则显示、设备勾选与滚轮模式一并关闭；拔出设备的恢复逻辑不变，重新插入或单击设备行可恢复生效。
- 新增全局滚轮模式：主菜单滚轮滑块下方内联「滚轮模式勾选 / 鼠标触发键 / 键盘触发键 / 灵敏度滑块」区（与设备子菜单同款样式），就地修改并保存到 `config.json` 顶层 `scroll` 字段；无规则生效时启用，规则生效期间停用（规则未配置滚轮模式时不回落）；修改全局配置视为手动调节，同样使生效规则失效；旧版配置中遗留的全局 `scroll` 字段恢复生效。

## 0.1.1

- 主菜单有规则设备行支持单击手动激活规则；手动激活后该设备被视为重新插入到 active 末尾，回退顺序同步更新。
- 支持为每个规则设置别名：设备子菜单增加别名编辑框，设置后替代设备名在主菜单、生效规则提示和托盘 tooltip 中显示。
- 修复滑动条在暗色主题下显示异常的问题，主菜单和子菜单的所有滑动条现在都能随系统主题正确显示。
