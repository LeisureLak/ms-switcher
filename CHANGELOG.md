# Changelog

## Unreleased

## 0.1.3 - 2026-09-12

- 启动时自动恢复上次生效的配置，不再每次从全局配置开始。
- 修复滚轮模式在睡眠唤醒、锁屏/安全桌面（UAC）后卡死或触发键失活的问题：触发键是否按住改为读取物理按键状态，事件位图中已物理松开的残留位自动对账清除，任意输入事件到达即自愈。

## 0.1.2 - 2026-09-08

- 新增完整的全局配置：全局指针速度、滚轮速度和滚轮模式持久化保存，旧配置自动补齐 Windows 默认值 10/3。
- 主菜单顶部改为显示当前生效配置，明确区分全局配置与设备特定配置；全局编辑区与当前设备配置完全脱钩。
- 设备区域新增「全局配置」项；设备项改为 toggle，点击设备切换到其配置，再次点击当前设备切回全局配置，当前设备拔出时也自动回到全局配置。
- 「恢复默认」现在会恢复全局指针/滚轮速度，并彻底关闭全局滚轮模式、清除鼠标和键盘触发快捷键。
- 全局滚轮模式支持鼠标触发键、键盘触发键和 2–200 像素/行灵敏度；设备配置生效期间修改全局设置不会影响当前系统速度或切换配置。
- 同型号多设备的生效勾选改为按设备实例 ID 精确匹配，并同步更新托盘提示、键盘导航和菜单合成输入测试。

## 0.1.1

- 主菜单有规则设备行支持单击手动激活规则；手动激活后该设备被视为重新插入到 active 末尾，回退顺序同步更新。
- 支持为每个规则设置别名：设备子菜单增加别名编辑框，设置后替代设备名在主菜单、生效规则提示和托盘 tooltip 中显示。
- 修复滑动条在暗色主题下显示异常的问题，主菜单和子菜单的所有滑动条现在都能随系统主题正确显示。
